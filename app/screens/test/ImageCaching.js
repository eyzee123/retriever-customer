import React from 'react';
import {Image, View} from 'react-native';

const ImageCaching = () => {
  return (
    <View
      style={{
        flex: 1,
      }}></View>
  );
};
export default ImageCaching;
